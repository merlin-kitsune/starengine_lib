# 平台属性:NeoForge 1.21.1(与消费方 astral_dice_multiloader/neoforge-1.21.1 同参数)
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.235
# ⚠️ 该键由 neoforge.mods.toml 模板写进 **loaderVersion** —— 那是 javafml **语言提供者**的版本槽,
#    比较的是 FML 语言提供者的版本(本机 NeoForge 21.1.235 报 `4.0.42`),**不是** NeoForge 版本带。
#    2026-09-17 实测:这里写成 NeoForge 版本带 `[21.1,21.2)` 会让 FML **拒载整个库 jar** ——
#      `Mod File …\.m2\…\starengine_lib-neoforge-1.21.1-1.0.0-SNAPSHOT.5.jar needs language provider
#       javafml:21.1 or above, and below 21.2 to load / We have found 4.0.42`
#      (两份崩溃报告:`run/1.21.1/crash-reports/crash-2026-09-17_20.41.28-fml.txt` 与
#       `…_20.42.17-fml.txt`,在消费方 `astral_dice_multiloader-next` 工作树内),
#      随后消费方 mod 被连带拒绝:`Mod astral_dice requires starengine_lib 1.0.0-SNAPSHOT.5 or above,
#      and below 2.0 / Currently, starengine_lib is not installed`。
#    ⇒ 与消费方 `astral_dice_multiloader-next/neoforge-1.21.1/gradle.properties:23` **同值 `[1,)`**。
#      NeoForge 版本带 `[21.1,21.2)` 属于模版里 `modId="neoforge"` **依赖**的位置(那里比的是版本),
#      本库该依赖当前就是 `[21.1,21.2)`,不需要也不应该搬到 loaderVersion。
loader_version_range=[1,)

## 库自身版本
# lib_version 用于 maven 坐标与 jar 文件名(纯 semver)
# 1.0.0-SNAPSHOT.16:消费方 26.1.2 线接入版 —— 库内 Java 源码零改动,仅随消费方「26.1.2 完整移植」
#   推进版本号(库的历史口径为「每个实质提交进 1 档」)。该线此前 pin 在 `.11`,本地留有
#   combat/HostileTargets、combat/PlayerHostilityTracker、target/SelectorTargets、target/SignSelectionGate
#   四个已被 `.15` 下沉取代的副本(冻结期未同步),本次一并删除并改指库包名。
#   必须 bump 的原因同前几版:该版本号不以 -SNAPSHOT 结尾(按 Maven 语义属普通版本),
#   Gradle 不把它当作 changing module —— 不 bump 则消费方会继续解析 mavenLocal 里的旧 jar。
# 1.0.0-SNAPSHOT.14:新增「玩家余额账本」底层(星币钱包功能的库侧实现):
#   - common/economy/EconomyStorage      平台存储 seam(读/写/离线读,不注册任何注册表条目);
#   - common/economy/StarEngineEconomy   对外 API(get/set/has/deposit/withdraw/transfer + 离线读,
#                                        供消费方与第三方模组如 FTB 联动;存储缺失时安全降级不抛异常);
#   - common/economy/StarCoinCommand     /starcoin add|set|remove|get|rank(含离线玩家榜单);
#   - 两平台各一份实现(玩家持久化 NBT + PlayerEvent.Clone 复制 ⇒ 死亡不掉),26.1.2 侧**未接入**
#     (故该线 API 返回 0/false,不抛异常)。
#   必须 bump 的原因同前几版:该版本号不以 -SNAPSHOT 结尾(按 Maven 语义属普通版本),
#   Gradle 不把它当作 changing module —— 不 bump 则消费方会继续解析 mavenLocal 里的旧 jar。
# 1.0.0-SNAPSHOT.11:充能冷却改口径带来的**源码不兼容 API 收敛**(2026-09-25):
#   ① 删除 `GameplayConstants.CHARGE_COOLDOWN_REDUCTION`(0.2;旧的「拥有充能即冷却 -20%%」比例减免);
#   ② 新增 `CHARGE_SIGN_COOLDOWN_CAP_SECONDS = 160` 与 `CHARGE_EFFECT_CARD_COOLDOWN_CAP_SECONDS = 20`
#      —— 改为「拥有充能时把**基础值**封顶」(立牌 180 秒→160 秒、效果牌 30 秒→20 秒),其它减免
#      (诡异骰子 -50%%、枪匠「精密技巧」120 秒)在封顶之后照常生效。
#   ③ 消费方三线(1.21.1 / 1.20.1 / 26.1.2)的 `ChargeManager` 同步改为按上限封顶;因该常量被删除
#      属**源码不兼容**变更,必须 bump 版本号,否则消费方会继续解析 mavenLocal 里仍带旧常量的 jar。
#   库内 Java 源码只动 `GameplayConstants` 这一个类,平台侧零改动。
# 1.0.0-SNAPSHOT.10:合并两批在途修复并把版本号统一到 `.10`(用户 2026-09-17 指令:
#    「starengine_lib 版本号更新至 1.0.0-SNAPSHOT.10」;与消费方 `2.0.0-SNAPSHOT.10` 的 patch 段同号):
#   ① 沿承 `.6` 的**库自身被 FML 拒载**根因修复 —— neoforge-1.21.1 的 `loaderVersion` 槽保持 javafml
#      语言区间 `[1,)`(第 7-19 行的说明;绝不改回 NeoForge 版本带 `[21.1,21.2)`,否则整个库 jar
#      会被 FML 拒载);forge-1.20.1 的 `loaderVersion=[47,48)` 与 `modId="forge"` 依赖
#      `[47.4.10,48)` 也保持 `.6` 时的取值不变。
#   ② 并入原以 `.7` 为号(未提交)的 **forge 资源包元数据修复**:新增
#      `forge-1.20.1/src/main/resources/pack.mcmeta`(`pack_format` 15),消除 Forge 1.20.1 加载界面
#      的 `File … failed to load a valid ResourcePackInfo` 警告及其「资源包不注册」静默失效面
#      (根因取证见 README §6「资源包元数据修复记录」与 CHANGELOG)。
#   ③ `.7` 只发布过 mavenLocal、从未进入 git(在途未提交),按仓库约定并入 `.10`,不单列 `.7` 小节。
#   库内 Java 源码零改动;必须 bump 的原因同前:该号不以 -SNAPSHOT 结尾(属普通版本),Gradle 不把它
#   当作 changing module —— 不 bump 则消费方会继续解析 mavenLocal 里会被 FML 拒载、且缺
#   `pack.mcmeta` 的旧 jar。
# 1.0.0-SNAPSHOT.6:修复**库自身被 FML 拒载**的根因(2026-09-17,以两份 FML 崩溃报告为证):
#   ① neoforge-1.21.1 的 loaderVersion 槽曾被错误地填成 NeoForge 版本带 `[21.1,21.2)`(本文件第 7 行
#      的详细说明与崩溃报告路径)⇒ 改为 javafml 语言区间 `[1,)`,与消费方同值;NeoForge 版本带仍由
#      模板里 `modId="neoforge"` 依赖的 `versionRange="[21.1,21.2)"` 承担(区间未变)。
#   ② 顺带对齐 forge-1.20.1 的已文档化口径:`loaderVersion` 由 `[47,)` 收敛为 `[47,48)`(语言主系列
#      区间),`modId="forge"` 依赖改用 build.gradle 从 `forge_version` 派生的精确区间 `[47.4.10,48)`。
#   ③ neoforge-26.1.2 的 `loader_version_range` 本就是 `[1,)`(未触发拒载),本次仅随版本线同号 bump。
#   必须 bump 的原因同 .5:该版本号不以 -SNAPSHOT 结尾(属普通版本),Gradle 不把它当作 changing
#   module —— 不 bump 则消费方会继续解析 mavenLocal 里那份**带错 loaderVersion、会被 FML 拒载**的
#   `.5` jar(`.5` 已发布到 mavenLocal)。本次只改元数据与模板占位,**库内 Java 源码零改动**。
# 1.0.0-SNAPSHOT.5:合并完成后的「过渡符号按期删除」(2026-09-17,主线 8f68482 已并入 dev-next):
#   ① 已删(删除前用 grep 举证:合并后消费方 neoforge-1.21.1 / forge-1.20.1 / neoforge-26.1.2
#      三线对这些符号零引用):
#      - EventTargetCollector.collectTargets / collectTeamTargets / collectMaids / isMaidOwnedBy;
#      - GameplayConstants.EVENT_RANGE / EVENT_APPLY_MAID / KOMACHI_EXTRA_PLAYS_CAP;
#      - effect/ReadyEffect(.4 下沉,合并后三线对其零引用)。
#   ② 已删(同批):event/AstralEventType / EventContext / EventEffect 三件套(三者互相引用,
#      必须同批删),以及 neoforge-26.1.2/build.gradle 里为 AstralEventType 留的 sourceSets
#      exclude 行(文件已删,该行只会成为无效残留)。
#      前提工序:合并后的消费方 AstralEventSystem.java 曾留 4 处指向本库的 import(方法体已不用,
#      但 Java 里未解析的 import 本身就是编译错误),先由消费方侧提交 7dc64cb 清除,库侧才可删。
#      注:.5 因此**重新发布过**一次(同版本号、不同字节)⇒ 消费方需 --refresh-dependencies 重新解析。
#   必须 bump 的原因同 .4:该版本号不以 -SNAPSHOT 结尾(属普通版本),Gradle 不把它当作 changing
#   module —— 不 bump 则消费方会继续解析 mavenLocal 里的 .4 旧 jar(仍带已删除的符号)。
# 1.0.0-SNAPSHOT.4:Phase 1d 收尾,把三线共享的自包含单元补齐下沉并固化判定口径:
#   ① 新下沉 common/effect/ReadyEffect —— 三线(neoforge-1.21.1 / forge-1.20.1 / neoforge-26.1.2)
#      字节完全一致、只依赖 MC API 与库内已有类的自包含单元中,**唯一**尚未进库者;
#   ② 三线的 53 个「字节完全一致」候选逐一判定,其余 52 个的排除理由
#      (非三线一致 / 依赖消费方专有类 / mixin)见 temp/sink-manifest-20260917.md;
#   ③ 主线(multi-1.20.1-1.21.1)在 dev-next 删除这批类**之后**对其余文件的改动已逐条核对:
#      GameplayConstants 取主线常量值 + 暂留过渡符号、EventTargetCollector 暂留全量实现
#      (合并前消费方仍在调用 collectTargets)、BossEntityUtil/SignActiveTriggeredEvent/
#      ModEffectRemoval/ActionBarManager 的差异为 shim 与注释,均属有意设计,本次不改这些类。
#   必须 bump 的原因同 .3:该版本号不以 -SNAPSHOT 结尾(属普通版本),Gradle 不把它当作
#   changing module —— 不 bump 则消费方会继续解析 mavenLocal 里的 .3 旧 jar(缺 ReadyEffect),
#   随后在编译期报「找不到符号」。
# 1.0.0-SNAPSHOT.3:在 .2 基础上做两处**二进制不兼容**收敛:
#   ① 完全移除 Cloth Config 前置与公共配置模块(StarEngineCommonConfig/StarEngineConfigs/
#      LegacyCommonTomlImporter/StarEngineConfigScreen 全部删除),配置文件回归消费方自己的
#      ModConfigSpec / ForgeConfigSpec;库内只留平台无关的值快照 seam。
#   ② GameplayConfigValues 由 13 字段收敛为 6 字段(与消费方 TOML 的配置项一一对应),
#      GameplayConstants.refresh() 改为 applyConfig(GameplayConfigValues)。
#   另新增 neoforge-26.1.2 平台子项目(MC 26.1.2 / NeoForge 26.1.2.109 / Java 25 / 无 Parchment),
#   common 由「两版同源」变为「三版同源」;26.1 的 ResourceLocation→Identifier 与
#   EntityType#is(TagKey)→builtInRegistryHolder() 两处差异已下沉进 shim(见 README §1.3)。
#   注:AstralEventType/EventContext/EventEffect 与 KOMACHI_EXTRA_PLAYS_CAP 属「合并后即删」的
#   过渡符号,当前**暂留**(尚未合并主线的消费方仍在引用),不在本版本删除。
#   必须 bump 的原因:该版本号不以 -SNAPSHOT 结尾(属普通版本),Gradle 不会当作 changing module,
#   不 bump 则消费方会继续解析到 mavenLocal 里的 .2 旧 jar(带已删除的类),编译期就会报找不到符号。
# 1.0.0-SNAPSHOT.2:在 .1 基础上把 Java 包名由 com.merlinkitsune.starengine 改为
#    com.merlinkitsune.starenginelib(规避名称法律风险),maven group 随之变为
#    com.merlinkitsune.starenginelib。这是一次**破坏性**变更:消费方全部 import 需同步改写。
#    必须 bump 到 .2 的原因:若不 bump,改名前的 .1 库 jar(modId 同为 starengine_lib、
#    版本号同为 1.0.0-SNAPSHOT.1、但是旧包名)能通过消费方的 versionRange 检查,
#    随后因找不到 com.merlinkitsune.starenginelib.* 而 NoClassDefFoundError 崩溃;
#    bump 后消费方区间下界提到 [1.0.0-SNAPSHOT.2,2.0),旧 jar 会被明确判为「缺必需前置」。
#    (已用 maven-artifact 3.8.5 实测:该区间 reject 1.0.0-SNAPSHOT.1,accept .2/.3/.10/1.0.0/1.1.0)
# 1.0.0-SNAPSHOT.1:本库首次对外发布(仓库托管到 GitHub)的版本号。
#   内容:效果/事件/目标选择/通用组件共享实现,以及平台无关的配置值快照 seam
#        (GameplayConfigValues + GameplayConstants.applyConfig)。
#   注意:SNAPSHOT 系列的 API **不**作语义化兼容承诺,升级消费方前先比对共享源码
#        (该豁免仅覆盖 SNAPSHOT 系列,已在 1.0.0 终止;1.x 起适用下方兼容性契约)。
#   注意:该版本号**不以 -SNAPSHOT 结尾**(而是 -SNAPSHOT.N),按 Maven 语义属于普通版本,
#        因此 Gradle 不会把它当作 changing module;库内容变更必须 bump 版本号,否则消费方会拿到缓存旧 jar。
# 1.0.0:**首个正式版**(由 1.0.0-SNAPSHOT.16 规范化而来,库内 Java 源码零改动)。
#   【2026-09-22 同日并入 —— 原拟作 1.0.1 单独发版,推送前经用户裁决**不单独发版、直接并入 1.0.0**】
#   移除 forge 侧的 Curios 前置(裁决原话「使其与另外两个版本保持一致」):
#   ① forge-1.20.1 的 mods.toml 删除 `modId="curios"` 依赖块(原 mandatory=true [5,6))⇒ **三平台产物
#      都不再声明 Curios 前置**;
#   ② 该侧依赖由 modImplementation 降为 **modCompileOnly** —— Curios 只在编译 item/CuriosCompat 时需要,
#      而库自身零调用它(它只是给消费方用的 shim);调用它的消费方自己声明 curios 必需;
#   ③ 纯放宽、不删改任何 public 类型/方法/字段/签名/语义 ⇒ 符合下方 1.x 兼容契约,
#      消费方 `starengine_lib_version_range=[1.0.0,2.0)` 无需改动;
#   ④ 三平台产物文件名保持 `starengine_lib-<平台>-1.0.0.jar`(**不产生 1.0.1**:1.0.1 从未推送、
#      从未发布,远端只有一个 tag `1.0.0`,其 Release 附件随本次 push 重刷)。
#   ⑤ 同日还**为第三条线补齐了一处缺失的平台实现**:neoforge-26.1.2 的 `NeoForgeEconomyStorage`
#      (钱包账本的平台存储;缺它时该线 `StarEngineEconomy.isAvailable()` 恒 false —— /starcoin 不注册、
#      拾取星币不被吸收进钱包、余额条恒 0)。该改动同样**原拟作 1.0.1**,经用户裁决**并入 1.0.0** ——
#      一处未发布的改动不占用两个版本号 ⇒ 远端仍只有 tag `1.0.0`。
#   ⚠️ **兼容性契约(全局约定,2026-09-22 起生效,三平台一体适用)**:
#     凡**第一位数字不变**的版本(同一主版本号,当前 = 1.x),一律**禁止任何破坏性更新** ——
#     不得删除/改名任何 public 类型、方法、字段、常量,也不得改变其可见性、签名或既有语义;
#     只允许**新增**(新类型/新成员/新可选入口)与不改变契约的行为修正。
#     确需破坏性变更时,**必须**把第一位加一(1.x → 2.x),并同步收紧消费方
#     `starengine_lib_version_range` 的下界 —— 破坏性变更**不允许**藏在次版本/补丁位里。
#     ⇒ 消费方区间 `[1.0.0,2.0)` 即本契约的机器可读表达:任何 1.x 版本都可原位替换,无需改消费方代码。
lib_version=1.0.0
# mod_version 用于 neoforge.mods.toml 中展示(带加载器后缀,与消费方约定一致)
mod_version=1.0.0+neoforge_1.21.1

## 模组元数据
mod_id=starengine_lib
mod_name=StarEngine Lib
mod_license=MIT License
mod_group_id=com.merlinkitsune.starenginelib
